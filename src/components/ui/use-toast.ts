
import { useToast, toast } from "@/hooks/use-toast";
import { toast as sonnerToast } from "sonner";

// Export both toast systems for compatibility
export { useToast, toast };

// Also export sonner toast for consistency
export { sonnerToast };
